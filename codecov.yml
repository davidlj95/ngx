coverage:
  status:
    patch:
      default:
        threshold: 5%
    project:
      default:
        threshold: 5%
comment:
  hide_project_coverage: false

codecov:
  require_ci_to_pass: false
  notify:
    wait_for_ci: false
