import { provideStandardManager } from '../utils/provide-standard-manager'

/**
 * Manages the {@link Standard.author} metadata
 * @public
 */
export const STANDARD_AUTHOR_METADATA_PROVIDER =
  provideStandardManager('author')
