import { MetadataRouteStrategy } from '../../routing/metadata-route-strategy'
import { OpenGraph } from '../open-graph'

export abstract class OpenGraphRouteStrategy extends MetadataRouteStrategy<OpenGraph> {}
